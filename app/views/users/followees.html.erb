<% provide :title, t("user.followees.title") %>

<%= render 'users/top', user: @user %>

<div class="row">
  <div class="col-md-12">
    <h3>
      <%= t("user.followees.title", count: @user.followees.size) %>
    </h3>
  </div>
</div>

<%= render 'users', users: @followees %>
