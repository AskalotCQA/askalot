<% provide(:title, t('user.index.title')) %>

<ul class="nav nav-tabs tab-navigation">
  <%= tab_link_tag_with_count t('user.tab.all'), :all, users_path(params.except(:page).merge tab: :all), User.count, analytics_attributes(:users, :switch, :all).merge(remote: true) %>
  <%= tab_link_tag_with_count t('user.tab.recent'), :recent, users_path(params.except(:page).merge tab: :recent), User.recent.count, analytics_attributes(:users, :switch, :recent).merge(remote: true) %>
</ul>

<%= render 'users', users: @users %>
