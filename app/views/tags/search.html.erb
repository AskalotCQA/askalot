<% provide(:title, t('tag.search.title')) %>

<%= render 'tags/searching', target: controller_name %>

<ul id="tags-tabs" class="nav nav-tabs tab-navigation">
  <%= tab_link_tag_with_count t('tag.tab.all'), :results, tags_path(params.slice(:q, :page)), @results.total_entries, analytics_attributes(:tags, :search, :results).merge(remote: true, data: {fade: '#tags'}) %>
</ul>

<%= render 'tags', tags: @results, remote: false %>
