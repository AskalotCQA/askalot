<% provide(:title, 'questions.index') %>

<div class="row">
  <div class="col-md-12">
    <section>
      <div class="page-header">
        <h1>Zobrazenie otázok</h1>
        <p><%= link_to "Ask Question", new_question_path, :class => "btn btn-success btn-lg", :class => "btn btn-success btn-lg" %></p>
      </div>
      <%= paginate @questions, :window => 3  %>
      <div>
        <%= render 'questions', questions: @questions %>
      </div>
      <%= paginate @questions, :window => 3  %>
    </section>
  </div>
</div>