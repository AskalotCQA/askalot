<section id="questions-controls">
  <%= icon_link_to :plus, t('question.new.feature'), new_question_path, id: :'new-question', class: 'btn btn-success pull-right' %>

  <%# TODO (smolnar) dry %>
  <ul class="nav nav-tabs">
    <%= tab_link_tag t('question.tab.new'), :'questions-new', questions_path(tab: :'questions-new', tags: params[:tags]), remote: true, data: { toggle: :tab } %>
    <%= tab_link_tag t('question.tab.favored'), :'questions-favored', questions_path(tab: :'questions-favored', tags: params[:tags]), remote: true, data: { toggle: :tab } %>
    <%= tab_link_tag t('question.tab.answered'), :'questions-answered', questions_path(tab: :'questions-answered', tags: params[:tags]), remote: true, data: { toggle: :tab } %>
  </ul>
</section>
<!-- TODO(zbell) add some navigation here: old / new q. etc. -->
