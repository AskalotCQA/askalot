<li>
  <div class="row">
    <div class="col-md-3">
      <div class="row">
        <div class="col-md-4">
          <%= content_tag :span, class: [:'question-metric', question_votes_coloring(question)] do %>
            <span class="question-value"><%= question.total_votes %></span>
            <%= t('count.vote', count: question.total_votes) %>
          <% end %>
        </div>
        <div class="col-md-4">
          <%= link_to question_path(question, anchor: :'question-answers'), class: [:'question-metric', question_answers_coloring(question)] do %>
            <span class="question-value"><%= question.answers.size %></span>
            <%= t('count.answer', count: question.answers.size) %>
          <% end %>
        </div>
        <div class="col-md-4">
          <%= content_tag :span, class: [:'question-metric', question_views_coloring(question)] do %>
            <span class="question-value">123</span>
            <%= t('count.view', count: 123) %>
          <% end %>
        </div>
      </div>
    </div>

    <div class="col-md-7">
      <div class="question-preview">
        <h4>
          <%= link_to question_title_preview(question), question_path(question) %>
        </h4>

        <%= render 'questions/labels', labels: question.labels %>

        <p>
          <%= question_text_preview question %>
        </p>
      </div>
    </div>

    <div class="col-md-2">
      <div class="question-author">
        <small><%= link_to_user question.author %></small>
        <a class="user-avatar" href="<%= user_path question.author.nick %>">
          <%= gravatar_image_tag question.author.gravatar_email, size: 64, class: :'img-circle' %>
        </a>
        <small class="text-muted"><%= question_creation_time question, placement: :bottom %></small>
      </div>
    </div>
  </div>
</li>
