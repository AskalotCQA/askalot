<%= form_messages key: tab %>

<%= render 'administration/categories/form', category: @category, tab: tab %>

<ol class="administration-categories">
  <% categories.each do |category| %>
    <li>
      <div class="row">
        <div class="col-xs-4">
          <%= link_to_category category %>
        </div>

        <div class="col-xs-6">
          <% category.tags.each do |tag| %>
            <span class="label label-inverse"><%= tag %></span>
          <% end %>
        </div>

        <div class="col-xs-2">
          <div class="pull-right">
            <%= render 'administration/categories/editing', category: category, tab: tab %>
          </div>
        </div>
      </div>
    </li>
  <% end %>
</ol>
