<% provide :title, t('administration.settings') %>

<div class="row">
  <div class="span6 offset3">

        <div class="form-group">
          <%= text_area :title, class: :'form-control', rows: 1 %>
        </div>

        <div class="form-group">
          <%= text_area :text, class: :'form-control', rows: 5 %>
        </div>

        <button type="submit" class="btn btn-success btn-lg"><%= t('user.sign_up') %></button>
  </div>
</div>
