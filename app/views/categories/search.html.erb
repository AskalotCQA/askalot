<% provide(:title, t('category.search.title')) %>

<%= render 'categories/searching', target: controller_name %>

<ul id="tags-tabs" class="nav nav-tabs tab-navigation">
  <%= tab_link_tag_with_count t('category.tab.all'), :results, tags_path(params.slice(:q, :page)), @results.total_entries, analytics_attributes(:categories, :search, :results).merge(remote: true, data: {fade: '#categories'}) %>
</ul>

<%= render 'categories', categories: @results, remote: false %>
