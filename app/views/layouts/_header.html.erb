<header id="header-navbar" class="navbar navbar-inverse navbar-fixed-top" role="navigation">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="icon-bar"></span><span class="icon-bar"></span><span class="icon-bar"></span>
      </button>
      <%= navbar_logo_tag default_title %>
      <% if user_signed_in? %>
        <a class="navbar-brand" href="/questions/new">Pridať novú otázku</a>
      <% end %>
    </div>
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav pull-right">
        <% if user_signed_in? %>
          <p class="navbar-text">
            <%= t('user.signed_in_as', login: current_user.login) %>
          </p>
          <%= navbar_dropdown_tag :'caret-down', current_user.email, '#', join: :append do %>
            <li><%= icon_link_to :heart, t('user.profile'), user_path(current_user.login), fixed: true %></li>
            <li><%= icon_link_to :cog, t('user.settings'), edit_user_registration_path, fixed: true %></li>
            <li><%= icon_link_to :'sign-out', t('user.sign_out'), destroy_user_session_path, fixed: true, method: :delete %></li>
          <% end %>
        <% else %>
          <li><%= icon_link_to :leaf, t('user.sign_up'), new_user_registration_path, fixed: true %></li>
          <li><%= icon_link_to :'sign-in', t('user.sign_in'), new_user_session_path, fixed: true %></li>
        <% end %>
      </ul>
    </div>
  </div>
</header>

<%= yield :header %>
