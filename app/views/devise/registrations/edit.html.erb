<% provide :title, t('user.account_settings') %>

<!-- TODO (harinek) localization) -->
<div class="row">
  <div class="col-md-4">
    <ul class="nav nav-pills">
      <li class="active"><a href="#user-profile" data-toggle="pill">Profil</a></li>
      <li><a href="#user-social" data-toggle="pill" >Sociálne siete</a></li>
      <li><a href="#user-privacy" data-toggle="pill">Súkromie</a></li>
      <li><a href="#user-account" data-toggle="pill">Nastavenia účtu</a></li>
    </ul>
  </div>
  <div class="col-md-8">
    <div class="tab-content">
      <div class="tab-pane active" id="user-profile">
        <%= render "edit_profile" %>
      </div>

      <div class="tab-pane" id="user-account">
        <%= render "edit_account" %>
      </div>

      <div class="tab-pane" id="user-privacy">
        <%= render "edit_privacy" %>
      </div>

      <div class="tab-pane" id="user-social">
        <%= render "edit_social" %>
      </div>
    </div>
  </div>
</div>
<!-- TODO (zbell) use modal for confirmation + user must enter login on submit -->
<!--
<section>
  <h2><%= t('user.account_deletion') %></h2>
  <%= button_to t('user.delete_account'), registration_path(resource_name), class: :'btn btn-danger', data: { confirm: "Are you sure?" }, method: :delete %>
</section>
-->
