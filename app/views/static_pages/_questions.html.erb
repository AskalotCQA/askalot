<ol class="list-unstyled">
  <% questions.each do |question| %>
    <li>
      <%= link_to truncate(question.title, length: 35, separator: ' '), question %>
      <small><%= truncate(question.text, length: 80, separator: ' ') %></small>
    </li>
  <% end %>
</ol>
