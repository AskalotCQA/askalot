<% provide :title, t('question.index.title') %>

<%= render 'filter' %>
<%= render 'index_header', category: @category %>
<%= render 'controls' %>
<%= render 'questions', questions: @questions, remote: true %>
