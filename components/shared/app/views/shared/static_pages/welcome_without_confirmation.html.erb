<% provide :title, t('user.welcome.title') %>

<div class="container">
  <div class="row">
    <div class="col-md-offset-3 col-md-6">
      <%= flash_messages %>
      <h1 class="page-header"><%= yield :title %></h1>
      <p class="lead"><%= t('devise.registrations.signed_up_without_confirmation') %></p>
    </div>
  </div>
</div>
