<% if news.any? %>
  <ol class="list-unstyled">
    <% news.each do |news| %>
      <li>
        <h4>
          <%= link_to t(news.title), news_index_path(tab: :'news')+"#news-#{news.id}" %>
        </h4>
        <div class="news-date">
          <small>
            <%= time_tag news.created_at.to_date, format: :default %>
          </small>
        </div>
        <%= new_text_preview news, length: 160 %>
      </li>
    <% end %>
  </ol>
<% end %>
