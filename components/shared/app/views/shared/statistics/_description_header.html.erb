<thead>
  <tr class="active">
    <th class="col-md-10"><%= title %></th>
    <th><%= content_tag :em, 'Min'.html_safe, tooltip_attributes(t('statistic.description_header.min')) %></th>
    <th><%= content_tag :em, 'x&#772;'.html_safe, tooltip_attributes(t('statistic.description_header.average')) %></th>
    <th><%= content_tag :em, 'Max'.html_safe, tooltip_attributes(t('statistic.description_header.max')) %></th>
  </tr>
</thead>
