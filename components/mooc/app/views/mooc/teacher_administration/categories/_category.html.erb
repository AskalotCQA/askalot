<td>
  <%= link_to_category category, tooltip_attributes(category.full_public_name).merge(target: '_blank') %>
</td>

<td>
  <% category.tags.each do |tag| %>
    <span class="label label-inverse"><%= tag %></span>
  <% end %>
</td>

<td>
  <%= check_box_tag 'shared[]', category.id, category.shared, tooltip_attributes(t 'activerecord.attributes.shared/category.shared').merge({class: 'treetable-checkbox'}) %>
</td>

<td>
  <div class="pull-right">
    <a href="<%= mooc.new_teacher_administration_category_path parent_id: category.id %>" class="text-muted text-undecorated">
      <%= tooltip_icon_tag :plus, t('teacher_administration.category.new.action'), placement: :top %>
    </a>

    <a href="<%= mooc.edit_teacher_administration_category_path category %>" class="text-muted text-undecorated">
      <%= tooltip_icon_tag :edit, t('teacher_administration.category.edit.action'), placement: :top %>
    </a>
  </div>
</td>
