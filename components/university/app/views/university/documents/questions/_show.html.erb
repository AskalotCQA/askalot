<div id="document-content">
  <a href="<%= university.index_document_questions_path(document_id: @question.document_id) %>" data-remote="true" class="btn btn-sm btn-default">
    <%= icon_tag :list, label: t('question.list') %>
  </a>

  <div id="document-question">
    <div class="row">
      <div class="col-xs-9 col-sm-7 col-md-10">
        <div id="question-title">
          <h3 class="question-title">
            <%= @question.title %>
            <%= evaluation_badge_tag @question if @question.evaluations.any? %>
          </h3>
          <%= render 'shared/questions/labels', labels: @labels %>
        </div>

        <div id="question-data">
          <div class="question-content">
            <%= render_markdown @question.text %>
          </div>

          <% if @question.attachments.any? %>
            <div class="question-attachments-section">
              <%= render 'shared/questions/attachments', question: @question %>
            </div>
          <% end %>

          <div class="question-details">
            <%= render 'shared/questions/details', question: @question %>
            <%= render 'university/documents/questions/editing', question: @question, remote: true %>
            <%= render 'university/documents/questions/deleting', question: @question, remote: true %>
          </div>

          <div id="question-evaluations">
            <%= render 'shared/questions/evaluations', question: @question %>
          </div>

          <div id="question-comments">
            <%= render 'university/documents/questions/comments', question: @question, remote: true %>
            <%= render 'university/documents/comments/form', commentable: @question, remote: true %>
          </div>
        </div>
      </div>

      <div class="col-xs-3 col-sm-2">
        <%= render 'shared/questions/voting', question: @question %>
        <%= render 'shared/questions/watching', question: @question %>
        <%= render 'university/documents/questions/evaluating', question: @question, remote: true %>
      </div>
    </div>
  </div>

  <div id="question-answers">
    <%= render 'university/documents/questions/answers', answers: @answers %>
  </div>

  <div id="question-answer" class="new-answer-form">
    <%= render 'university/documents/answers/form', question: @question, answer: @answer, remote: true %>
  </div>
</div>
